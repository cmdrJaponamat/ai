# ai-control-plane Recovery

## project_path

`/home/japonamat/ai`

## purpose

Operational control plane для локальных AI/Codex-сессий, system work, recovery trail, project registry, MCP tooling и PostgreSQL context store.

## current_state

- Репозиторий: `/home/japonamat/ai`
- Remote: `git@github-auto:cmdrJaponamat/ai.git`
- Главные правила и bootstrap-контекст:
  - `WORKFLOW.md`
  - `ASSISTANT_BOOTSTRAP.md`
  - `RULES.json`
  - `PROJECT_CONTEXTS.md`
- База контекста: PostgreSQL `ai_context`
- Legacy индекс проектов: `projects.db`
- MCP слои:
  - `mcp/ai_control`
  - `mcp/context_tools`
  - `mcp/phototrap_tools`
- LightRAG bootstrap существует, но текущая выбранная линия: `PostgreSQL + MCP`, без локальной LLM/embeddings как обязательной зависимости.

## important_recent_changes

- Создан `ai_context` в PostgreSQL.
- Добавлен `context-tools` MCP слой поверх `ai_context`.
- Добавлены `kb_*` tools для project projection/status/active tasks.
- `phototrap-tools` начал писать audit snapshots в `ai_context.snapshots`.
- Launcher-скрипты Codex переведены на явный `gpt-5.4` с `model_reasoning_effort=low`.
- `/home/japonamat` переведен в `trusted` в `~/.codex/config.toml`, чтобы новая сессия могла нормально подхватывать MCP-конфиг.

## current_next_steps

- Добиться, чтобы новая Codex runtime-сессия реально видела MCP tools как callable namespaces.
- Перевести `codex-ai-launcher-json` на MCP-first prompt:
  - выбранный проект передавать как metadata;
  - project recovery не инлайнить по умолчанию;
  - использовать markdown только как fallback, если MCP недоступен.
- Добавить query-tools поверх `ai_context.documents`.
- Улучшить `kb_project_status_compact`, чтобы `focus` был инженерным, а не repo-oriented.
- Добавить отдельный MCP tools слой для самого `ai-control-plane`.

## key_commands

```bash
codex mcp list
codex mcp get context-tools
/home/japonamat/ai/mcp/.venv/bin/python3 /home/japonamat/ai/mcp/context_tools/cli.py kb-project-status-compact Photo_Trap
/home/japonamat/ai/mcp/.venv/bin/python3 /home/japonamat/ai/mcp/context_tools/cli.py context-list-snapshots Photo_Trap
psql -h 127.0.0.1 -U japonamat -d ai_context
```

## source_of_truth

- Для правил и workflow: markdown/JSON файлы в `/home/japonamat/ai`.
- Для проектного индекса: `PROJECT_CONTEXTS.md` + legacy `projects.db` + synced `ai_context.projects`.
- Для derived context: `ai_context.documents`, `ai_context.kb_projections`, `ai_context.snapshots`.
- Для system work trail: `actions.log` и `recovery/*.md`.

